# Encryption in transit for Amazon HealthLake<a name="encryption-in-transit"></a>

Amazon HealthLake uses TLS 1\.2 to encrypt data in transit through the public endpoint and through backend services\.